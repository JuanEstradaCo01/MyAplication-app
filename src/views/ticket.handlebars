<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Document</title>
</head>
<body style="background-color: grey;">

    <header style="background-color: rgb(2, 136, 80);
                   font-size: 2rem;
                   height: 6rem;
                   display: flex;
                   justify-content: center;
                   align-items: center;
                   flex.direction: column;">
        <h2>🌫My aplication</h2>
    </header>

    <h1 style="text-align: center;">Detalles de la compra</h1>

    <div style="background-color: white;border: 3px solid black;display: flex;justify-content: center;align-items: center;flex-direction: column;margin: 10rem">
        <h2>Ticket de compra</h2>
        <p><strong>Ticket No. </strong>{{data.code}}</p>
        <p><strong>Fecha: </strong>{{data.purchase_datetime}}</p>
        <p><strong>Correo: </strong>{{data.purchaser}}</p>
        <div style="margin-left: 1rem;border: 2px solid black;">
            {{#each cartProducts}}
                <ul>
                    <li><strong>{{this.name}}</strong> X<strong>{{this.quantity}} </strong>{{this.size}}</li>
                    <p>Precio U. <strong>${{this.price}}</strong> - Total: <strong>${{this.totalAllQuantity}}</strong></p>
                </ul>
            {{/each}}
        </div>
        <h6>Total bruto: ${{data.totalPrices}}</h6>
        <p>IVA 19%</p>
        <h4><strong>Total compra: </strong>${{data.amount}}</h4>
    </div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>